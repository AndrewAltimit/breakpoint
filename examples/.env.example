# Breakpoint Environment Variables
# Copy to .env and fill in values

# API authentication token (required for event ingestion)
BREAKPOINT_API_TOKEN=changeme

# GitHub webhook secret (for verifying webhook payloads)
BREAKPOINT_GITHUB_SECRET=

# Log level (trace, debug, info, warn, error)
RUST_LOG=info
